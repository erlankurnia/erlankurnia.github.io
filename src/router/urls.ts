import type IUrl from "@/helper/interfaces/IUrl";

const urls: IUrl = {
    about: '/who_iam',
    credit: '/references',
    notes: 'https://kiroku.erlankurnia.laness.id',
    note: 'https://kiroku.erlankurnia.laness.id',
    // notes: '/notes',
    // note: '/note',
    project: '/project',
    projects: '/projects',
    uses: '/uses',
    equipment: '/equipment',
};

export default urls;